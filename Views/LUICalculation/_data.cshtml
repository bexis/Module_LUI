@model  BExIS.Modules.Lui.UI.Models.DataModel

<div id="download">

</div>

<div id="data">
    <table id="regResults">
        <thead>
            <tr>
                @for (int c = 0; c < Model.Data.Columns.Count; c++)
                {
                    <th>
                        @Model.Data.Columns[c].ColumnName
                    </th>
                }
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Data.Rows.Count; i++)
            {
                <tr>
                    @for (int c = 0; c < Model.Data.Columns.Count; c++)
                    {
                        <td>
                            @Model.Data.Rows[i][c].ToString()
                        </td>

                    }
                </tr>
            }

        </tbody>
    </table>
</div>


<script>
    $(document).ready(function () {
        $('#regResults').DataTable();
    });

</script>